# 오운 작업일지 — 2026-04-26

---

## ⚠️ 이전 미기록 항목 (2026-04-23 ~ 24)

### 1. CRM 권한 컬럼 추가

**위치:** DB — `trainers` 테이블  
**변경 내용:**
- `trainers.crm_permissions` JSONB 컬럼 추가
- CRM 포털 접근 제어 기반 데이터 구조 확보

**관련 파일:** `supabase/migrations/009_crm_permissions.sql` (추정)  
**커밋:** `09cd431`

---

### 2. CRM 결제 수단 선택 + 상품 관리 모달 + 회원 수정 UI 개선

**위치:** CRM 포털  
**변경 내용:**
- 결제 수단 선택 기능 추가
- 상품 관리 모달 구현
- 회원 수정 UI 개선 (편집 UX 정비)

**커밋:** `f573691`

---

### 3. 어드민 포털 — 포털별 관리탭 구조 개편 + 플랜 관리 기능 추가

**위치:** 어드민 포털  
**변경 내용:**
- 어드민 포털 탭 구조를 포털별(트레이너/커뮤니티 등)로 개편
- 트레이너별 플랜(free/pro/unlimited) ON/OFF 토글 기능 추가
- 플랜 카드: 상단 뱃지 항상 노출, `overflow:hidden` 제거로 잘림 방지

**관련 커밋:**
| 커밋 | 내용 |
|------|------|
| `40a7e3f` | admin 포털 포털별 관리탭 구조 개편 + 플랜 관리 기능 추가 |
| `54d0c4d` | 플랜 관리 ON/OFF 오류 수정 + 플랜별 노출 토글 추가 |
| `7b0038a` | 플랜 카드 뱃지 잘림 수정 — overflow:hidden 제거 |
| `633c3c4` | 플랜 OFF 블러 시 상단 뱃지는 항상 노출되도록 수정 |

---

## 📅 2026-04-26 (일)

### 1. 랜딩페이지 전면 개편 + 어드민 랜딩 관리 기능 추가

**위치:** `src/pages/Landing.jsx`, `src/pages/AdminPortal.jsx`  
**배경:** 서비스 출시 대비, 전환율 최적화를 위한 랜딩페이지 전면 재설계

**추가된 섹션 (9개)**

| 섹션 | 내용 |
|------|------|
| 헤드라인 | 주타겟 메시지 교체, 강조 카피 개선 |
| 문제 인식 | 트레이너가 겪는 Pain Point 나열 |
| 솔루션 | 오운이 해결하는 방식 3~4가지 카드 |
| 숫자 지표 | 주요 수치(가입자 수, 수업일지 수 등) |
| 후기 | 실제 트레이너 리뷰 카드 슬라이더 |
| 카카오 반응 | 카카오 채널 반응 캡처 형태 사회적 증거 |
| 타겟 분기 | 트레이너용 / 회원용 각각 CTA |
| 요금제 | PLANS 배열 기반 플랜 카드 |
| FAQ | 자주 묻는 질문 아코디언 |

**어드민 랜딩 관리 탭**
- 어드민 포털에 `랜딩관리` 탭 추가
- `app_settings` 테이블 기반 데이터 실시간 반영
  - 통계 수치 수정 (CRUD)
  - 트레이너 후기 추가/수정/삭제
  - 카카오 메시지 관리
  - FAQ CRUD
- 요금제는 코드 내 `PLANS` 배열로 분리 → 코드 수정 없이 관리 가능 구조

**관련 파일:** `src/pages/Landing.jsx` (+636줄), `src/pages/AdminPortal.jsx` (+243줄)  
**커밋:** `51c3865`

---

### 2. 커뮤니티 유저 접근 권한 관리 기능

**위치:** 어드민 포털 > 커뮤니티 > 유저 탭 / `src/pages/CommunityPortal.jsx`  
**변경 내용:**

**어드민 측**
- 유저 목록에 권한 설정 버튼 추가 → 모달 오픈
- 모달 내 기능:
  - **커뮤니티 차단(banned)** 토글
  - **추가 역할(extra_roles)** 체크박스 (educator, instructor 등)
  - 유저 목록에 현재 상태 뱃지 표시 (기본 / +N권한 / 차단)

**DB 마이그레이션 (`20260424_community_admin_permissions.sql`)**
```sql
ALTER TABLE community_users
  ADD COLUMN IF NOT EXISTS admin_permissions JSONB DEFAULT '{}';
-- 필드: banned (bool), extra_roles (text[])
```

**커뮤니티 포털 측**
- 차단 유저 접근 시 차단 안내 화면 표시 (로그아웃 버튼 포함)
- `extra_roles` 기반 카테고리 열람·작성 권한 동적 적용

**`permissions.js` 확장**
- 복수 역할 동시 지원 헬퍼 함수 추가 (`hasAnyRole`, `mergeRolePermissions`)

**커밋:** `3ef9b26`

---

### 3. 커뮤니티 게시글 이미지 첨부 기능

**위치:** `src/pages/CommunityPortal.jsx`, `src/styles/community.css`

**글쓰기 화면**
- 사진 첨부 버튼 추가 (📷)
- 최대 5장, 파일당 10MB 제한
- 썸네일 미리보기 그리드 + 개별 삭제(×) 버튼
- `community-posts` Supabase Storage 버킷에 업로드
- 등록 중 `uploading` 상태 표시

**글 상세 화면**
- 첨부 이미지 그리드 표시 (2열 레이아웃)
- 이미지 클릭 시 원본 새 탭 오픈

**피드 카드**
- 첫 번째 이미지 썸네일 표시
- 이미지 2장 이상 시 `+N` 카운트 뱃지

**DB 마이그레이션 (`20260424_post_images.sql`)**
```sql
ALTER TABLE community_posts
  ADD COLUMN IF NOT EXISTS image_urls TEXT[];
```

**커밋:** `0bcc8b4`

---

### 4. 브랜드명 'TrainerLog' → '오운' 전면 교체

**배경**  
서비스 출시명 확정: **오운 (Owun)**  
- '오운완(오늘 운동 완료)' 밈 연상
- '소유(Own)'의 프리미엄 이중 의미
- "오운 켜봐", "오운으로 예약해" — 발음 자연스럽고 세련

**변경 범위**

| 포털 | 변경 위치 |
|------|-----------|
| 랜딩페이지 | 네비 로고, 솔루션·리뷰 섹션 텍스트, 푸터 로고·버전 |
| 트레이너 포털 | 탑바, 랜딩 스크린 로고(×3), 수업 알림 제목, 온보딩 텍스트, 상태 배지, AI 설정 안내 |
| 회원 포털 | 로그인 로고, 탑바, 회원 랜딩, PDF 헤더/푸터, 인쇄 헤더/푸터 |
| 관리자 포털 | 로그인 로고, 탑바 |
| 커뮤니티 포털 | 헤더 로고, 로그인 로고, 탑바 로고 |
| 수업 리포트 | 헤더 브랜드, 푸터 브랜드 |
| 약관/개인정보/환불 | 네비 로고, 본문 서비스명 |
| 푸시 알림 | SW(`sw.js`), `push.js`, SQL 기본값(`007_web_push.sql`) |
| HTML 타이틀 | `index.html`, `admin-portal.html`, `trainer-app.html`, `member-portal.html`, `report.html` |

**로고 스타일** (기존 `TRAINER[LOG]` 박스 패턴 유지)
```jsx
// 변경 전
TRAINER<span style={{background:'#c8f135',...}}>LOG</span>

// 변경 후
오<span style={{background:'#c8f135',...}}>운</span>
```

**커밋:** `791ff04`, `b2123de`

---

### 5. 이용약관 / 개인정보처리방침 / 환불정책 페이지

**위치:** `src/pages/Terms.jsx`, `src/pages/Privacy.jsx`, `src/pages/Refund.jsx`  
**변경 내용:**
- 3개 법적 문서 페이지 신규 생성
- 각 페이지: 오운 로고 네비, 섹션별 조항 구성
- `App.jsx`에 `/terms`, `/privacy`, `/refund` 라우트 연결
- Landing.jsx 푸터 링크와 연결 완료

**커밋:** `b2123de`, `6370057`

---

## 📁 커밋 요약

| 커밋 | 날짜 | 내용 |
|------|------|------|
| `51c3865` | 04-26 | feat: 랜딩페이지 전면 개편 + 어드민 랜딩 관리 기능 추가 |
| `0bcc8b4` | 04-26 | feat: 커뮤니티 게시글 이미지 첨부 기능 추가 |
| `3ef9b26` | 04-26 | feat: 커뮤니티 유저 접근 권한 관리 기능 추가 |
| `791ff04` | 04-26 | feat: 앱 브랜드명 'TrainerLog' → '오운' 전면 변경 |
| `b2123de` | 04-26 | feat: 누락된 로고/브랜드 표기 '오운'으로 추가 변경 |
| `6370057` | 04-26 | feat: 이용약관·개인정보·환불정책 라우트 추가 |

---

## 📁 변경 파일 목록

| 파일 | 변경 유형 |
|------|---------|
| `src/pages/Landing.jsx` | 전면 개편 (+636줄) |
| `src/pages/AdminPortal.jsx` | 랜딩 관리 탭 + 커뮤니티 권한 관리 (+373줄) |
| `src/pages/CommunityPortal.jsx` | 이미지 첨부 + 권한 관리 (+160줄) |
| `src/pages/TrainerApp.jsx` | 브랜드명 교체 (로고·알림·텍스트) |
| `src/pages/MemberPortal.jsx` | 브랜드명 교체 + 인쇄 헤더/푸터 |
| `src/pages/Report.jsx` | 브랜드명 교체 |
| `src/pages/Terms.jsx` | 신규 생성 |
| `src/pages/Privacy.jsx` | 신규 생성 |
| `src/pages/Refund.jsx` | 신규 생성 |
| `src/App.jsx` | 약관 라우트 3개 추가 |
| `src/lib/permissions.js` | 복수 역할 헬퍼 추가 |
| `src/styles/community.css` | 이미지 첨부 스타일 (+104줄) |
| `index.html` / `*.html` | 브랜드명 타이틀 교체 (5개 파일) |
| `public/sw.js` | 푸시 알림 브랜드명 교체 |
| `src/lib/push.js` | 알림 타이틀 브랜드명 교체 |
| `supabase/migrations/007_web_push.sql` | 알림 기본값 교체 |
| `supabase/migrations/20260424_post_images.sql` | 신규 생성 |
| `supabase/migrations/20260424_community_admin_permissions.sql` | 신규 생성 |
