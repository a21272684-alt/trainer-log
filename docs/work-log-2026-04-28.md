# 오운 작업일지 — 2026-04-28

---

## 📅 2026-04-28 (화)

### 1. 커뮤니티 포털 — 회원가입 역할 항목 추가

**위치:** `src/lib/permissions.js`, `src/pages/CommunityPortal.jsx`  
**배경:** 기존 회원가입 역할 선택 화면에 헬스장 대표·교육강사만 있었고, 일반 회원과 트레이너 역할이 누락됨

**변경 내용**
- `ROLE_META` — `member` label `'회원'` → `'일반 회원'`, desc `'나에게 맞는 트레이너 구하기 · ...'`
- `ROLE_META` — `trainer` desc `'수업받을 회원 구하기 · ...'`
- 회원가입 역할 선택 필터를 `auth === 'google'` 조건에서 명시 목록 `['member', 'trainer', 'gym_owner', 'educator']`로 변경
- 역할 카드 표시 순서 고정: **일반 회원 → 트레이너 → 헬스장 대표 → 교육강사**

---

### 2. 커뮤니티 포털 — 카테고리 탭 개편

**위치:** `src/lib/permissions.js`, `src/pages/CommunityPortal.jsx`, `src/pages/AdminPortal.jsx`  
**배경:** "직원 구인" 탭이 "트레이너 채용"과 역할 중복, 트레이너가 레슨 모집을 올릴 탭 부재

**삭제**
- `trainer_seeks_member` (직원 구인) — `gym_seeks_trainer`(트레이너 채용)과 기능 중복

**추가**
- `trainer_lesson_recruit` (레슨 회원 모집 💪)
  - write: `['trainer']` 전용
  - view: 일반 회원 포함 전체 역할
  - hint: 레슨 종류·대상·가능 시간대·비용 등

**어드민 동기화**
- `AdminPortal.jsx` — `COMM_CAT_LABEL`, `CAT_COLOR` 매핑 업데이트

---

### 3. 커뮤니티 포털 — 게시글 등록 버그 수정

**위치:** `src/pages/CommunityPortal.jsx`, `supabase/migrations/20260428_community_post_bucket.sql`  
**증상:** 레슨 회원 모집 카테고리에서 "등록하기" 클릭 시 오류 발생

**원인 분석**
- `createPost()` insert 시 `status` 필드 누락 — `loadPosts`는 `status = 'active'`로 필터링하나 insert에 미포함
- `community-posts` 스토리지 버킷 미생성 (게시글 이미지 첨부 시 필요)
- 에러 메시지가 추상적("등록 중 오류가 발생했습니다")이어서 디버깅 불가

**수정 내용**
- `createPost` insert에 `status: 'active'` 명시 추가
- 에러 발생 시 DB 실제 메시지 토스트 노출 + `console.error` 추가
- `supabase/migrations/20260428_community_post_bucket.sql` 마이그레이션 추가
  - `community-posts` 버킷 생성 (public)
  - `community_posts.status` 컬럼 `DEFAULT 'active'` 설정

**DB 마이그레이션 (`supabase/migrations/20260428_community_post_bucket.sql`)**
```sql
INSERT INTO storage.buckets (id, name, public)
VALUES ('community-posts', 'community-posts', true)
ON CONFLICT (id) DO NOTHING;

ALTER TABLE community_posts
  ALTER COLUMN status SET DEFAULT 'active';
```

> ⚠️ Supabase 대시보드 SQL 에디터에서 실행 필요
