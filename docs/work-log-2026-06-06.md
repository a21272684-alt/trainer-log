# 작업 로그 — 2026-06-06

> **트레이너 공개 프로필 마무리(동의 흐름 재설계·디자인·인증데이터) + 운영 버그 hotfix 다수 + 회원 식단 조회 + 출결 정책 + 리더보드 복구**
> 마지막 로그(2026-05-28) 이후의 작업을 모음. 베타 운영 중 들어온 피드백 반영 + 트레이너 공개 프로필 MVP 완결 + 숨어있던 RLS/게이트 버그 청소.

---

## 한눈에 보기

| 영역 | 결과 |
|---|---|
| 신규 마이그 | 060 (트랜스포메이션 동의 흐름) · 061 (인증데이터 RPC) · 062 (diet_logs strict RLS) · 063 (출결 정책) · 064 (주간 리더보드 RPC) |
| 운영 버그 hotfix | AI 일지 크레딧 게이트 / 랜딩 후기 사진 얼굴 잘림 / 리더보드 본인만 노출 / 후기 marquee 즉시 반복 |
| 신규 기능(배포 노출) | 회원 식단 조회(트레이너) · 출결 정책 + 회원 동의 · 후기 사진 세로 위치 슬라이더 |
| 신규 기능(dev 게이팅) | 트레이너→회원 비포애프터 업로드 → 회원 1탭 동의 흐름 / 공개 프로필 인증데이터·애니메이션 |
| 자산 | 인스타 스토리 승인제 안내 이미지 |
| 운영 영향 | hotfix 4건·신규 기능 3건이 배포 앱에 노출. 그 외 dev 게이팅 유지 |

---

## 1. 공개 프로필 — 동의 흐름 재설계 (2026-05-29)

회원 직접 업로드 부담 + 트레이너↔회원 사진 주고받는 번거로움을 줄이면서 회원 본인 동의 기록은 유지하는 절충안으로 전환.

- **방향 전환:** 회원 업로드 → **트레이너가 업로드, 회원이 1탭 동의/거부**
- **얼굴 가리기 토글 제거** (코드상 무동작이었음 — declarative였을 뿐) → 안내 문구만
- **삭제 정책 단순화:** "내리기 + 영구삭제" → **영구삭제 단일** (강한 확인 다이얼로그). 서버 비용·UX 명확성 ↑

**060_member_transformations_consent_flow.sql**
- `status` 에 `pending` / `declined` 추가 (호환 위해 draft/revoked 도 유지)
- `expires_at` (트레이너 업로드 시 7일 안내용)
- 트레이너 INSERT RLS

**구성요소:**
- `MemberTransformationUpload.jsx` (신규, 트레이너) — 회원 선택 + 업로드 → pending
- `TransformationsShare.jsx` (회원) 재작성 — 동의 대기 1탭 처리 + 공개 중 영구삭제
- `PublicProfile`: 비포애프터 **1열 큰 카드(220px)**, 결과 라벨 강조

## 2. 공개 프로필 — 디자인 다듬기

- **세련된 리디자인:** 다크 그라데이션 + 라임 글로우 + 라임 링 아바타 + 라임 액센트 섹션 헤더 + 그림자 카드 + **하단 고정 CTA 바**
- **순수 CSS 애니메이션 (라이브러리 0):** 랜딩의 `useInView`/`FadeUp` 패턴 차용. 스크롤 등장(섹션 stagger 130/260ms), 히어로 글로우 호흡, 아바타 등장+ring pulse, 콘텐츠 fade, 카드 hover lift, 버튼 hover/press, CTA 바 등장
- 등장 속도 완화 (.7s → 1.1s, ring pulse 2.4 → 2.8s) — "돋보이는" 속도로 튜닝
- `prefers-reduced-motion` 존중
- dev 진입 버튼 3개 통일: 솔리드 라임 그라데이션(`#d9f99d → #c8f135`) + 라임 글로우 + 검정 글씨. 🪪 → 👤/💪 (시스템 폰트 호환)

## 3. 공개 프로필 — 오운 인증 데이터 (061)

**get_public_trainer_stats(handle)** SECURITY DEFINER RPC.
- 누적 수업일지 / 관리 회원 / 활동 기간 — 집계값만, PII 0
- `is_public` + `show_stats` 게이트 → NULL 반환 시 박스 숨김
- 추측성 출석률/평균유지 대신 정직한 3지표로 확정

## 4. AI 일지 크레딧 게이트 hotfix

베타 트레이너 제보: 크레딧 200개 충전 후에도 "유료 플랜 전용"으로 잠김.

- 원인: 버튼 게이트가 `canUse('ai_journal')`(플랜) 만 보고 크레딧 무시. Free 안내문 "AI 일지 월 20회"와도 모순
- 수정: 게이트를 `canUse('ai_journal') || credits > 0` 로 완화. `generateLog` 의 크레딧 차감 + 월한도 안전장치는 그대로

## 5. 회원 식단 조회 + diet_logs strict RLS

배포된 트레이너 앱에 식단 뷰가 없어 회원이 회원앱에 기록해도 트레이너가 못 봄 + 028 의 `diet_logs` 가 allow_all 이라 anon 노출이라는 숨은 P0.

- **MemberDietView.jsx** (트레이너 회원상세 rtab 'diet') — 날짜·끼니별 + kcal/매크로 합산, 읽기 전용
- **062_diet_logs_strict_rls.sql** — allow_all 제거. 회원 본인 OR 담당 트레이너 SELECT, 본인만 쓰기
- **admin 제어:** AdminPortal `FEATURE_DEFS` 에 `diet_view` 추가 → 기본 무료(`free.diet_view:true`), 나중에 free 토글 끄면 즉시 유료 전환 가능

## 6. 랜딩 후기 사진 얼굴 잘림

**3단계로 발전했습니다.**
1. **즉시 fix:** `.ld-mq-photo` 에 `object-position:center top` → 인물 사진 얼굴 우선 보존
2. **admin 위치 선택:** `photo_position`(상단/중앙/하단) 토글 추가 — `app_settings.landing_reviews` JSON 필드라 마이그 불필요
3. **연속 슬라이더로 발전:** 3프리셋이 거친 케이스 → 세로 포커스 0~100% 슬라이더(`object-position: 50% Y%`). 옛 `photo_position` 자동 매핑(상단=0, 중앙=50, 하단=100)으로 호환

## 7. 출결 정책 (063) — 노쇼/결석 감정 마찰 완화

문제 재정의: 노쇼 시 트레이너가 수동 차감 → 회원 입장에선 "트레이너가 임의로 깎았다" 개인 감정. 해결의 본질은 **나쁜 소식의 전달자를 사람 → 시스템으로** 바꾸는 것.

**063_attendance_policy.sql**
- `attendance` 에 `status`(attended/noshow/cancelled/late) + `session_deducted` + `note` (기존 행=attended, 호환)
- `attendance_policies` (트레이너별: 취소시한·노쇼차감(1/0)·정책문구)
- `members.attendance_policy_agreed_at` (회원 본인 동의 시각)
- 트레이너 INSERT RLS, 정책 RLS(트레이너 쓰기 + 그 회원 열람)

**트레이너:** `AttendancePolicyEditor` (취소 시한/노쇼 차감/정책 문구 — 자동 생성 + 편집), 출석 탭에 노쇼·사전취소 버튼 + 캘린더 색 구분 + 출결 내역 배지. 노쇼+정책차감 시 `done_sessions` 자동 차감/복원.

**회원:** `MemberAttendanceView` — 정책 + 본인 출결 내역(트레이너와 동일) + "확인했어요" 동의 토글. 수업일지 탭에 "📋 출결 내역·정책 보기" 진입(항상 노출).

## 8. 인스타 스토리 — 승인제 안내 이미지

`docs/mockups/insta-story-approval.html` — 1080×1920 9:16. 다크 + 라임 글로우, "지금은 승인제로 운영하고 있어요", 이유(악성 어뷰징 방지), 3단계(가입 → 가입 후 DM/카톡 문의 → 승인 완료), 강조 박스("가입만 하면 승인 X"), 문의처(📩 인스타 DM @iruscales · 💬 카톡 채널 @ownapp).

## 9. 주간 리더보드 — 전체 복구 + opt-in 실명 (064)

베타 트레이너 제보: 리더보드에 본인 1명만 보임.

- **git 이력 추적**으로 원인 확정: 원래 전체 트레이너 대상이었으나 이후 "직원 급여 + CRM 고도화" 일괄 작업에서 **gym_id 격리가 사용자 지시 없이 끼어듦**. + 050/051 strict RLS 로 cross-trainer 직접 조회 차단 → 본인만 노출
- **결정 흐름:** 익명/닉네임은 "조작 의심"으로 신뢰 파괴 → **실명 + opt-in** 으로 절충 (전원 실명 = 신뢰 유지, 동의자만 노출 = 프라이버시·법적 안전)

**064_weekly_leaderboard.sql**
- `trainers.leaderboard_opt_in` 컬럼 (기본 false, 본인 self-update RLS)
- `get_weekly_leaderboard()` SECURITY DEFINER RPC — 전체 트레이너 집계 (RLS 우회 안전). 이름은 opt-in + 본인만 노출. 전체 총계는 익명 집계로 전체 포함

**TrainerApp:** `loadLeaderboard` 직접쿼리 2개 → RPC 1콜로 교체. 설정 리더보드 헤더에 **"내 이름 공개" 토글**.

## 10. 랜딩 후기 marquee — 한 사이클 내 동일인 반복 제거

증상: 후기 3명 등록 시 한 바퀴 끝나기 전에 같은 사람이 또 등장.
원인: `reps=ceil(6/n)` 보강(넓은 화면 채우기용)이 한 세트 안에서 중복 노출 → 즉시 반복처럼 보임.
수정: 보강 제거(`oneSet = reviews`). 2벌 복제는 keyframe `-50%` 무한 루프 위해 유지. 카드 간격 16→28px, marquee 28→40s(모바일 45→55s)로 호흡감 ↑.

---

## 브랜치 / PR

| 브랜치 | 상태 | 비고 |
|---|---|---|
| `feat/trainer-profile-editor` | 머지됨 | 동의 흐름 재설계까지 누적 |
| `feat/trainer-diet-view` | 머지됨 | 062 적용됨 |
| `fix/landing-review-photo-crop` | 머지됨 | 슬라이더까지 |
| `feat/attendance-policy` | 머지됨 | 063 적용 필요 (멱등) |
| `docs/insta-story-approval` | docs only | 머지 여부 무관 |
| `hotfix/ai-journal-credit-gate` | 머지됨 | 베타 트레이너 정상화 확인 |
| `fix/leaderboard-global-optin` | 머지됨 | 064 적용 후 정상 확인 |
| `fix/landing-marquee-no-repeat` | PR 대기 | 머지 시 Vercel 즉시 반영 |
| `docs/work-log-2026-06-06` | 본 로그 | |

---

## 적용 체크리스트 (사용자)

- [x] 060 (트랜스포메이션 동의 흐름)
- [x] 061 (인증 데이터 RPC)
- [x] 062 (diet_logs strict RLS)
- [x] 063 (출결 정책)
- [x] 064 (리더보드 RPC + opt-in)
- [ ] 머지 대기: `fix/landing-marquee-no-repeat`

## 다음 후보

- 수익화 설계(끌어올리기 크레딧 / Pro 일회성 언락) — 베타 트래픽 안정 후
- 회원 변화 공유 분석(노쇼율, 인증데이터 신규 지표)
- 출결 정책 Phase 2: 수업 전 사전 리마인더 자동화(기존 Web Push 재활용), 노쇼율 통계
- AdminPortal community 탭 복구(052 패턴 admin RPC)
- `member_posts` 잔여 정리 검토

---

## 통계 (2026-06-06)

| 지표 | 값 |
|---|---|
| 신규 마이그 | 5건 (060/061/062/063/064) |
| 운영 hotfix | 4건 |
| 신규 컴포넌트 | MemberTransformationUpload / TransformationsShare(리팩) / MemberDietView / AttendancePolicyEditor / MemberAttendanceView |
| 빌드 | portal + admin 통과 |
| 락인 | 0 |
| 외부 라이브러리 추가 | 0 (애니메이션도 순수 CSS) |
